Deque : Vector와 유사하나 가장 앞의 데이터를 추가/삭제할 때 용이하다.(이때 순번은 다시 정의된다) 다만 앞과 뒤의 삽입/삭제를 제외하고는 vector보다 성능이 좋지 않다.

Linked list : 컨테이너 내부에서 각 요소의 인덱스를 저장하는 것이 아닌 요소끼리의 연결관계만 저장하는 자료구조이다.
특정 인덱스로 접근하는 것이 불가능하여 이를 해결하기 위해 iterator의 사용이 필요하다.
