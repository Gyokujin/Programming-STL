#include <iostream>
#include <set>
#include <algorithm>
using namespace std;

// 주어진 배열에서 중복 요소를 제거한 후, 오름차순으로 정렬하세요.
int main()
{
	set<int> numbers = { 1,3,5,7,9,9,7,4,6,12,10,2,2,2 };

	for (int num : numbers)
	{
		cout << num << endl;
	}
}
