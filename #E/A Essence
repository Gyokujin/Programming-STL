STL 알고리즘 : STL의 작업을 정의해 놓은 함수로 대표적으로 탐색(searching), 정렬(sorting), 계수(counting)가 있다.
알고리즘의 특성에 따라 데이터가 변하는 변경 알고리즘과 변하지 않는 불변경 알고리즘으로 나뉜다.

탐색 알고리즘
find : find(start, end, value)로 선언하며 컨테이너 내에서 찾으려는 데이터의 index를 반환한다. 반복자를 사용해야 하며 중복된 값이 있을 경우 가장 먼저 발견된 index를 반환한다.
